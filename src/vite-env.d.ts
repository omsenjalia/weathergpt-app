/// Ambient declarations for the Expo port. `process.env` is provided by the
/// Expo/Metro runtime (EXPO_PUBLIC_* vars are inlined at bundle time).

declare const ExpoSpeechRecognitionModule: Record<string, unknown> | undefined;
