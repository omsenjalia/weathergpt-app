# Flutter app configuration — public, bundled into the app. NOT a secret store.
# Create/edit .env in the omsenjalia/weathergpt-app repository root.
# Replace this placeholder with your actual reachable backend URL.
# Never add Google/OAuth/IMD/AccuWeather/Groq/TypeSafe credentials here.
BACKEND_URL=https://your-backend.example.com
